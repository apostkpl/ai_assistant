services:
  kyc-backend:
    build:
      context: .
      dockerfile: ./kyc/Dockerfile
    container_name: kyc-service
    ports:
      - "8020:8020"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    command: >
      bash -c "uv run alembic upgrade head && uv run uvicorn kyc.main:app --host 0.0.0.0 --port 8020"
    networks:
      - ai-assistant-network
      - global-db-network
      - global-proxy-network

networks:
  ai-assistant-network:
    external: true
  global-db-network:
    external: true
    name: db-net
  global-proxy-network:
    external: true
    name: proxy-net